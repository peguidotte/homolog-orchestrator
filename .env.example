# Aegis Homolog Orchestrator - Environment Variables
# Copy this file to .env and fill in the values
# NEVER commit the actual .env file to version control

# Database Configuration
DB_PASSWORD=your_database_password_here

# RabbitMQ Configuration
RABBITMQ_PASSWORD=your_rabbitmq_password_here

# Encryption Configuration
# IMPORTANT: This key is used to encrypt sensitive data (tokens, passwords)
# - Must be at least 16 characters (32 recommended for AES-256)
# - Must be kept secret and consistent across deployments
# - If changed, existing encrypted data will become unreadable
# Generate a secure key: openssl rand -base64 32
ENCRYPTION_SECRET_KEY=your_32_character_secret_key_here!!

